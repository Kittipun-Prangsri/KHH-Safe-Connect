{
  "name": "khh-safe-connect-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.5.2",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --filter @khh/web build",
    "start": "pnpm --filter @khh/web start",
    "deploy": "pnpm --filter @khh/web deploy",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "next": "^14.1.0",
    "typescript": "^5.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@firebase/util",
      "protobufjs"
    ]
  }
}
